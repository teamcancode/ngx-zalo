import { LoginZaloDirective } from './login-zalo.directive';

describe('LoginZaloDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginZaloDirective();
    expect(directive).toBeTruthy();
  });
});
