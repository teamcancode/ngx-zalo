import { LogoutZaloDirective } from './logout-zalo.directive';

describe('LogoutZaloDirective', () => {
  it('should create an instance', () => {
    const directive = new LogoutZaloDirective();
    expect(directive).toBeTruthy();
  });
});
